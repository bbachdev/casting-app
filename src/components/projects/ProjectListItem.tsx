import { Card, CardContent } from '../ui/card';

export default function ProjectListItem() {
  return (
    <Card>
      <CardContent>
        <h2>ProjectListItem</h2>
        <p>ProjectListItem</p>
      </CardContent>
    </Card>
  )
}